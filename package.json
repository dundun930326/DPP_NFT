{
  "name": "dpp-nft-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Static DPP NFT demo with a local Pinata metadata upload proxy",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
